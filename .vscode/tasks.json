{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build for debug",
            "type": "shell",
            "command": "cargo build --bin tri_led",
            "problemMatcher": [
                "$rustc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}